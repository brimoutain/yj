using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Curtain : Object
{
    //可能可以做一个没被破环时的动画
    private void Update()
    {
        if (isInteracted)
        {
            //等待命名。。。
            //anim.SetBool(name, false);
        }
    }
}
